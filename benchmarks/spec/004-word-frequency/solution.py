"""Word frequency counting."""


def word_frequencies(text):
    """Return a dict mapping each word in `text` to how many times it occurs.

    Case-insensitive ("The" and "the" are the same word) and punctuation is
    not part of a word ("cat," and "cat" are the same word). Words are
    separated by whitespace. The empty string returns an empty dict.
    """
    raise NotImplementedError
