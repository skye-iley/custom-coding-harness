"""Balanced-bracket checking."""


def is_balanced(s):
    """Return True if every (), [] and {} in `s` is properly matched and
    nested. Other characters are ignored. '([{}])' is balanced; '([)]' and
    '(' are not.
    """
    raise NotImplementedError
