"""Square matrix rotation."""


def rotate_90(matrix):
    """Return a NEW matrix, `matrix` rotated 90 degrees clockwise.

    `matrix` is a square (N x N) list of lists; the input must not be
    mutated. Example: [[1, 2], [3, 4]] -> [[3, 1], [4, 2]].
    """
    raise NotImplementedError
