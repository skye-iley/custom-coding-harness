"""Leap year calculation."""


def is_leap_year(year):
    """Return True if `year` is a leap year under the Gregorian rule:

    divisible by 4, except century years (divisible by 100), which must
    also be divisible by 400 to count.
    """
    raise NotImplementedError
