name = "gemini-3.5-flash"
context_window = 1048576
output_token_limit = 65536

# USD per million tokens. Snapshot — verify (follow-up.md: best-effort estimate).
[pricing]
input = 0.30
output = 2.50
cache_read = 0.075    # cached-input read (caching not enabled yet; field recorded per M1 addendum)
priced_as_of = "2026-06-23"

# Watt-hours per token, rough estimate (placeholder — see follow-up.md).
[energy]
per_input_token = 0.0001
per_output_token = 0.0003
