name = "gpt-5.5"

# USD per million tokens. Snapshot — verify (follow-up.md: best-effort estimate).
[pricing]
input = 1.25
output = 10.0
cache_read = 0.125    # cached-input read (caching not enabled yet; field recorded per M1 addendum)
priced_as_of = "2026-06-23"

# Watt-hours per token, rough estimate (placeholder — see follow-up.md).
[energy]
per_input_token = 0.0003
per_output_token = 0.0009
