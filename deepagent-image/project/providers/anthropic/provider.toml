# Anthropic Claude via langchain-anthropic (native init_chat_model prefix).
api_key_env = "ANTHROPIC_API_KEY"
requires_key = true
priority = 2
default_model = "claude-haiku-4-5"
# prefix =
# base_url_env =
pricing = "rate_table"   # per-model [pricing] tables; cost from models/*.toml
